# خلاصة تحسين SEO و GEO — salfordkw.shop
تاريخ التحليل الأصلي: 2026-08-05
تاريخ آخر دمج ومراجعة (فحص مباشر لكل ملفات الموقع): 2026-08-06 — **تمت إعادة الفحص والتأكد مرة ثالثة بنفس التاريخ بعد رفع الموقع فعلياً على GitHub**

> **ملاحظة لمن سينفّذ التعديلات:** هذا الملف يوثّق كل اقتراح مع السبب والملف المتأثر ودليل من البيانات. البنود المعلَّمة ✅ **تم التأكد من تنفيذها فعلياً بفحوصات مباشرة منفصلة لملفات الموقع بتاريخ 2026-08-06** ولا حاجة لإعادة تنفيذها.

> ⚠️ **درس مهم من فحص v1.0:** عند ترحيل صور 4.A من ibb.co، الفحص الأول اقتصر على ملفات الصفحات الرئيسية وصفحات الكتالوج فقط، وفات عليه فحص مجلد `/blog/` وملف `products.json` (مصدر بيانات JS مستخدم بعدة صفحات). بعد رفع الموقع فعلياً وفحص نسخة GitHub الحيّة، اكتُشف أن 4 مقالات مدونة و`products.json` لا تزال فيها روابط ibb.co قديمة — **تم إصلاحها فوراً بنفس الفحص**. الدرس: أي بند "ترحيل/استبدال رابط" لاحقاً يجب أن يُفحص عبر **كل** الملفات (`**/*.html` + `*.json` + `*.xml`) وليس فقط الصفحات الظاهرة للزائر.

> **إصدار الملف:** الرقم الحالي **v1.1**. عند أي تعديل لاحق (إضافة/حذف/تغيير أي بند)، يُزاد رقم الإصدار بمقدار 1 تلقائياً — v1.1 → v1.2 → ... **صيغة اسم الملف:** `seo-vX.X-improvement-plan.md` (الرقم مباشرة بعد `seo` وقبل بقية الاسم).

> ⚠️ **تصحيح مهم بتاريخ 2026-08-06:** البند 3.B في الإصدار السابق (v0.8) ذكر خطأً أن `sajjad.html`, `majalis.html`, `athath.html`, `works.html` "لا تزال تعتمد على جافاسكربت لعرض المنتجات". بعد فحص أدق، تبيّن أن هذه الملفات الأربعة **كانت بالفعل تحتوي محتوى HTML ثابتاً كاملاً** (كل المنتجات مكتوبة مباشرة بالكود، وJS يعمل فقط كتحسين إضافي/تحديث). الخطأ كان في الاعتماد على وجود استدعاء `fetch()` كدليل وحيد دون التحقق من محتوى الـ`div` الفعلي. **الفجوة الحقيقية الوحيدة المتبقية كانت في `index.html` فقط** (قسم "آخر أعمالنا" بالصفحة الرئيسية) — وتم إصلاحها الآن (راجع القسم 2).

> ⚠️ **تعليمات إلزامية لمن سينفّذ التعديلات فعلياً على ملفات الموقع:**
> - **عدّل ملفاً واحداً فقط في كل مرة، ثم توقف** واعرض ملخصاً بما تم تعديله بالضبط، ثم انتظر رد المستخدم.
> - **لا تكمل للملف التالي إلا إذا كتب المستخدم صراحة "اكملي"** أو ما يعادلها بوضوح.
> - ترتيب التنفيذ يتبع القسم الأخير من هذا الملف، ملفاً واحداً في كل خطوة.

---

## 1. ملخص بيانات Search Console (من التحليل الأصلي 2026-08-05)

- الفترة: آخر 3 أشهر (2026-05 إلى 2026-08) — إجمالي النقرات: 20 | الظهور: 460 | CTR: ~4.3%
- 99% من الزيارات من الكويت وعبر الجوال. متوسط الموضع: 7–8.
- الاستهداف الكلمات المفتاحية صحيح ومحلي؛ المشكلة كانت في: قلة الصفحات المفهرسة، سرعة التحميل، وعمق المحتوى.
- **ملاحظة:** هذه بيانات قديمة (قبل التوسّع الكبير في الموقع). يُنصح بسحب تقرير GSC جديد بعد تنفيذ البنود العالقة لقياس الأثر الفعلي.

---

## 2. ✅ ما تم إنجازه فعلياً منذ الإصدار v0.6 (تم التأكد بالفحص المباشر)

الموقع تطوّر بشكل كبير منذ كتابة الخطة الأصلية. هذه البنود **منجزة ولا حاجة لإعادتها**:

| البند الأصلي | الحالة | الدليل من الفحص |
|---|---|---|
| 3.1 — `reviews.html` في sitemap | ✅ تم | موجودة في `sitemap.xml` |
| 3.3 — محتوى رقيق بالصفحات الداخلية | ✅ تم | `sajjad.html` 2045 كلمة، `majalis.html` 1758، `athath.html` 1516، `works.html` 2438 (كانت 200-300 فقط) |
| 3.4 — عنوان `index.html` طويل | ✅ تم | العنوان الآن "سالفورد للأثاث \| مجالس ديكور وسجاد تركي - توصيل لكل الكويت" (~58 حرفاً) |
| 3.5 — `loading=lazy` بالصور | ✅ تم بشكل صحيح | أول صورة `eager` والباقي `lazy` (منطق صحيح، لا حاجة لتعديل) |
| 3.6 — `noindex` لـ `logo.html`/`watermark.html` | ✅ تم | موجود `<meta name="robots" content="noindex, nofollow">` في كلا الملفين |
| 3.2 — ضغط الصور الثقيلة | ✅ تم إلى حد كبير | أثقل صورة الآن 356KB (كانت 1712KB) — لا تزال بضع صور فوق هدف 150-200KB لكن التحسن كبير جداً |
| 5.1 — عرض المنتجات كـ HTML ثابت | ✅ تم جزئياً (مهم — راجع القسم 3 أدناه) | صفحات الكتالوج الفردية (37 صفحة) أصبحت HTML ثابت بالكامل، لكن صفحات المحور (`sajjad.html`, `majalis.html`, `athath.html`, `works.html`, وقسم آخر المنشورات في `index.html`) **لا تزال** تجلب المحتوى عبر `fetch('/products.json')`/`posts.json` بجافاسكربت |
| 5.2 — `sameAs` وتوحيد NAP | ✅ تم | `sameAs` موجود في schema بروابط انستقرام/تيك توك/واتساب |
| 6.1 — GA4/GTM وتتبع الأحداث | ✅ تم | `gtag` مع `G-8WRQFKRYXP` + تتبع `phone_click` و`whatsapp_click` |
| 6.2 — صفحة FAQ عامة مستقلة | ✅ تم | `faq.html` موجودة |
| 6.3 — زر واتساب + اتصال سريع | ✅ تم | موجود بعدة أماكن مع تتبع gtag |
| 6.5 — مقالات مدونة بالمواضيع المقترحة | ✅ تم | 7 مقالات في `/blog/` تطابق المواضيع المقترحة (اختيار السجاد، سجاد تركي مقابل صيني، الفرق موكيت/سجاد، ألوان المجالس، تنظيف السجاد، ترندات ديكور المجالس، أفضل موكيت) |
| 7.1 — صفحة "عن سالفورد" | ✅ تم | `about.html` موجودة |
| 7.2 — صفحات منتج مستقلة + Product schema | ✅ تم | 37 صفحة في `/catalog/` |
| 7.3 — `HowTo` schema | ✅ تم | موجودة في `index.html`, `sajjad.html`, `majalis.html`, `athath.html`, `works.html` |
| 7.4 — `Breadcrumb` schema بكل الصفحات | ✅ تم | مؤكد في كل الصفحات التسويقية + كل صفحات الكتالوج + كل مقالات المدونة |
| 7.5 — أسماء صور وصفية + Image Sitemap | ✅ تم | أسماء الملفات بالعربي الوصفي (مثل `مجلس-ديكور-صباح-الناصر-سالفورد.webp`)، ووسوم `<image:image>` موجودة فعلاً في `sitemap.xml` |
| 5.4 — ملف `llms.txt` | ✅ تم | موجود ويتضمن سنة التأسيس (2016) وعدد المشاريع (+790) |
| Google Business Profile | ✅ تم (تأكيد من صاحب الموقع — إجراء خارج ملفات الموقع) | ⚠️ **يجب المتابعة اليومية للتأكد من موافقة جوجل على الملف/النشاط**، لأن ملفات النشاط التجاري الجديدة على Google Business قد تُعلَّق أو تحتاج مراجعة يدوية من جوجل قبل ظهورها فعلياً في نتائج البحث وخرائط جوجل (Local Pack) |
| 3.C — 4 صفحات كتالوج ناقصة من `sitemap.xml` | ✅ تم (2026-08-06) | أُضيفت `majalis-11.html`, `majalis-12.html`, `sajjad-13.html`, `sajjad-16.html` — الآن كل الـ37 صفحة موجودة في `sitemap.xml` |
| 3.B — `sajjad.html`, `majalis.html`, `athath.html`, `works.html` | ✅ تم فعلياً (كانت منجزة مسبقاً، لم تُكتشف إلا اليوم) | محتوى المنتجات/الأعمال مكتوب ثابتاً بالكامل في HTML لهذه الملفات الأربعة؛ الفجوة الوحيدة كانت في `index.html` |
| 3.B (تكملة) — قسم "آخر أعمالنا" في `index.html` | ✅ تم (2026-08-06) | أُضيف محتوى ثابت لآخر 5 مشاريع (نفس بيانات `posts.json`) داخل `#home-works-grid`، مع إبقاء JS كتحسين إضافي فقط لتحديث المحتوى تلقائياً |
| 4.A — ترحيل جميع صور المنتجات من `i.ibb.co` للاستضافة المحلية | ✅ تم (2026-08-06) | جميع الـ25 رابط ibb.co الفريد المستخدم بالموقع (كانت موزّعة على 27+ صفحة كتالوج، الصفحات المحورية، `og:image`، وسكيما `index.html`) استُبدلت بروابط محلية `salfordkw.shop/products/`. الفحص الأول فات عليه `products.json` و4 مقالات مدونة (`blog/*.html`) — تم اكتشافها وإصلاحها عند فحص نسخة GitHub الحيّة. **تأكيد نهائي:** صفر نتائج لـ`i.ibb.co` بكامل الموقع (`**/*.html` + `*.json` + `*.xml`) باستثناء نص placeholder غير فعّال داخل حقل إدخال بلوحة `admin.html` (صفحة `noindex`، لا يحتاج تعديل) |

---

## 3. 🔴 أولوية عالية — لا يزال معلّقاً

### 3.A — صفر روابط بين صفحات الكتالوج والمدونة — ✅ **تم الإصلاح (2026-08-06)**
**ما نُفِّذ فعلياً:** إضافة قسم "مقالات ذات صلة" في كل صفحة من صفحات الكتالوج الـ37 (يربط لمقال أو اثنين من المدونة حسب الفئة: سجاد → مقالات السجاد، مجالس → مقالات المجالس)، وإضافة روابط عكسية لصفحتين كتالوج محددتين داخل كل مقال من المقالات السبعة (وليس رابطاً عاماً لصفحة الفئة فقط كما كان سابقاً).
**⚠️ ملاحظة مهمة اكتُشفت أثناء التنفيذ:** فئة **"أثاث وديكور" (athath) ليس لها أي مقال مدونة مخصص** — صفحات `athath-01` إلى `athath-09` (9 صفحات) رُبطت مؤقتاً بأقرب مقال ذي صلة (`majlis-decor-trends.html`) بدل تركها بلا رابط إطلاقاً، لكن هذا حل مؤقت وليس مثالياً. **يُنصح بإضافة مقال مدونة واحد على الأقل مخصص للأثاث/الديكور الداخلي** (مثال: "كيف تختار قطع الديكور المناسبة لمجلسك") ليحل محل الرابط المؤقت — بند جديد يُضاف للقسم 4 أدناه.

---

## 4. 🟡 أولوية متوسطة — لا يزال معلّقاً

### 4.B — `dateModified` ناقص من سكيما الكتالوج — ✅ **تم الإصلاح (2026-08-06)**
**ما نُفِّذ فعلياً:** إضافة `"dateModified":"2026-08-06"` لبيانات `Product` schema في جميع صفحات الكتالوج الـ37. بالإضافة لذلك، أُضيف نص ظاهر "آخر تحديث للسعر: 6 أغسطس 2026" بجانب خانة السعر، لكن **فقط في الصفحات الـ12 التي تعرض سعراً فعلياً محدداً بالمتر** (مثل "9 دينار / المتر").
**⚠️ اكتشاف مهم أثناء التنفيذ يُصحّح فهم البند 9.1 من الإصدارات السابقة:** الصفحات الـ25 المتبقية (أغلبها `athath` و`majalis`) **لا ينقصها بيانات بالخطأ** — سعرها الفعلي معروض كـ"يُحدَّد عند المعاينة — تواصل معنا"، وهذا **نموذج تسعير حقيقي ومقصود** لمنتجات مخصصة (تفصيل حسب المقاس). لذلك **لم تتم إضافة نص "آخر تحديث للسعر" لها ولا حقل `price` وهمي بالسكيما**، لأن إضافة سعر غير حقيقي تُعتبر بيانات منظّمة مضللة (قد يُعاقبها جوجل بدل أن يفيدها). هذا يُلغي فعلياً البند القديم "9.1 — تعبئة price الناقص في 27 صفحة" كما كان مطروحاً — **ليس كل نقص بيانات خطأً يستوجب تعبئة، بعضه واقع تجاري حقيقي يجب توثيقه لا "تصحيحه".**

### 4.C — تحميل خط Tajawal (اقتراح جديد — ⚠️ نصف صحيح، تفصيل مهم)
**الوضع الفعلي:** الموقع يستخدم بالفعل `preconnect` و`display=swap` مع خط Tajawal — أي أن الإصلاح المحدد الذي اقترحه الـ AI (font-display) **منفّذ مسبقاً** وليس مشكلة. الجزء المتبقي فعلاً هو أن `<link rel="stylesheet">` يبقى طلباً متزامناً يؤخر شوي عرض الـ CSS، رغم التحسينات الموجودة.
**الإصلاح (أدق مما اقتُرح):** تحميل ملف الخط بـ preload + تفعيله بعد التحميل (async trick)، أو استضافة الخط محلياً بدل الاعتماد الكامل على Google Fonts. أولوية متوسطة فقط — ليست عالية كما وُصفت، لأن أهم جزء (font-display) موجود أصلاً.

### 4.D — بنود سابقة لم تُنفَّذ بعد
- **6.6 — Preload للصورة الرئيسية:** لم يُنفَّذ بعد (لا يوجد `rel="preload"` في أي صفحة تم فحصها). أما **Minify CSS/JS**: الكود الحالي مكتوب أصلاً بشكل مضغوط نسبياً (بدون فراغات زائدة)، فالفائدة من تشغيله بأداة minify رسمية محدودة الآن — يبقى تحسين اختياري منخفض الأثر، وليس Preload الذي أثره أكبر على LCP.
- **3.2 (تكملة):** بعض الصور لا تزال فوق 200KB (أثقلها 356KB) — تحسين إضافي بسيط.
- **3.8 / 3.9 — التحقق من حالة الفهرسة الفعلية عبر GSC وطلب فهرسة يدوية:** إجراء يدوي خارج ملفات الموقع، لا يزال معلقاً ولا يمكن التأكد منه بفحص الملفات.
- **3.10 — تمايز بنية الصفحات المحورية:** يستحق مراجعة الآن بعد أن أصبح المحتوى أطول — تأكد أن كل صفحة تحتوي عنصراً فريداً (جدول أسعار، سؤال مختلف) لا مجرد تكرار بصياغة معدّلة.

### 4.E — لا يوجد مقال مدونة مخصص لفئة "الأثاث والديكور" (اكتُشف أثناء تنفيذ 3.A بتاريخ 2026-08-06)
**الوضع:** المدونة الحالية (7 مقالات) تغطي السجاد والموكيت والمجالس فقط. فئة `athath` (9 صفحات كتالوج) لا يوجد لها أي محتوى معرفي مخصص، وتم ربطها مؤقتاً بمقال مجالس غير مطابق تماماً كحل مؤقت (راجع 3.A).
**الإصلاح المقترح:** كتابة مقال واحد على الأقل مخصص للأثاث/الديكور الداخلي (مثال: "كيف تختار قطع الديكور المناسبة لمجلسك أو ديوانيتك")، ثم تحديث روابط "مقالات ذات صلة" في صفحات `athath-01` إلى `athath-09` لتشير له بدل الحل المؤقت.

---

## 5. 🟢 أولوية منخفضة — لا يزال معلّقاً

### 5.A — حقل `knowsAbout` ناقص من السكيما الرئيسية (اقتراح جديد — ✅ تم التحقق، صحيح)
**الوضع:** فحصت الـ JSON-LD الكامل في `index.html` (`LocalBusiness`/`FurnitureStore` ممتاز وشامل otherwise) — لا يوجد حقل `knowsAbout`.
**الإصلاح المقترح:**
```json
"knowsAbout": ["سجاد تركي", "موكيت", "مجالس ديكور", "ديوانيات", "ثيل صناعي"]
```
إضافة رخيصة (سطر واحد بالسكيما).

### 5.B — بنود منخفضة الأولوية من الخطط السابقة (بلا تغيير في الحالة)
- الإيموجي داخل `<h1>`: لا يزال موجوداً — **قرار مقصود، لا حاجة لتعديله** (كما ورد بالخطة الأصلية).
- صفحات مناطق منفصلة (6.7): لم تُنفَّذ — تبقى بحذر، تبدأ بمنطقة أو اثنتين فقط بمحتوى مختلف فعلياً.
- صفحة عروض/خصومات (6.9)، `humans.txt` (7.10)، الفيديوهات (يوتيوب/انستقرام embed): لم تُنفَّذ، تبقى منخفضة الأولوية كما بالخطة الأصلية.

---

## 6. ترتيب التنفيذ المحدَّث (v1.1)

1. ~~إضافة الصفحات الأربع الناقصة لـ `sitemap.xml`~~ ✅ تم (3.C)
2. ~~توليد HTML ثابت لصفحات المحور~~ ✅ تم (3.B)
3. ~~ترحيل صور المنتجات من `i.ibb.co` للاستضافة المحلية~~ ✅ تم (4.A)
4. ~~ربط صفحات الكتالوج بمقالات المدونة ذات الصلة والعكس~~ ✅ تم (3.A) — مع فجوة مكتشفة: لا مقال لفئة الأثاث (راجع 4.E)
5. ~~إضافة `dateModified` بسكيما صفحات الكتالوج + نص "آخر تحديث للسعر" الظاهر~~ ✅ تم (4.B) — فقط للصفحات ذات السعر الثابت الفعلي
6. كتابة مقال مدونة مخصص لفئة الأثاث والديكور (4.E) — جديد
7. إضافة `knowsAbout` لسكيما `index.html` (5.A) — سطر واحد
8. تحسين تحميل خط Tajawal (preload/استضافة محلية) (4.C)
9. Preload للصورة الرئيسية + ضغط الصور المتبقية فوق 200KB (4.D)
10. مراجعة حالة الفهرسة الفعلية عبر Search Console وطلب فهرسة يدوية لأي صفحة متأخرة (3.8/3.9) + مراجعة تمايز بنية الصفحات المحورية (3.10)
11. (لاحقاً، بحذر): صفحات مناطق منفصلة (6.7)، صفحة عروض بشرط التحديث المستمر، فيديوهات embed

---

## 7. متابعة دورية مطلوبة (ليست تعديل ملفات، بل إجراء متكرر)

- **Google Business Profile:** تفعيله تم ✅، لكن **يجب تفقّد حالة الموافقة/النشر يومياً** من لوحة تحكم Google Business حتى يظهر فعلياً في نتائج البحث وخرائط جوجل (Local Pack) — الملفات الجديدة أحياناً تُعلَّق لمراجعة يدوية من جوجل قبل الموافقة النهائية.
