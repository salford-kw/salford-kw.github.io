# تعليمات للـ AI التالي — حالة تنفيذ خطة السيو (salford-kw.shop)

> هذا الملف يوثّق **ما تم تنفيذه فعلياً** حتى الآن من `seo-v0.6-improvement-plan.md`، بالترتيب المحدد في القسم 8 من ذلك الملف، حتى يعرف أي AI أو منفّذ آخر من أين يكمل دون تكرار العمل أو تخطي خطوة.

## ⚠️ القاعدة الإلزامية التي يجب اتباعها (من ملف الخطة الأصلي)
- **عدّل ملفاً واحداً فقط في كل مرة، ثم توقف.**
- بعد كل تعديل، اعرض للمستخدم ملخصاً دقيقاً بما تم تغييره في ذلك الملف تحديداً، ثم انتظر رده.
- **لا تكمل للملف التالي إلا إذا كتب المستخدم صراحة "اكملي"** (أو ما يعادلها بوضوح مثل "كمل"/"التالي"). أي رد آخر أو صمت لا يُعتبر إذناً بالمتابعة.
- اتبع "ترتيب التنفيذ" في القسم 8 من `seo-v0.6-improvement-plan.md` — بند واحد (وملف واحد ضمنه) في كل خطوة.

## ⚠️ قاعدة إضافية إلزامية — تجميع كل التعديلات الخاصة بنفس الملف دفعة واحدة
**أضافها صاحب الموقع صراحة، ويجب على أي AI أو منفّذ لاحق الالتزام بها:**
- عندما يحين دور تعديل ملف معيّن (مثلاً `sajjad.html`)، **يجب أولاً مراجعة كل بنود الخطة (`seo-v0.6-improvement-plan.md`) التي تؤثر على هذا الملف تحديداً** — وليس فقط البند الحالي في ترتيب التنفيذ — ثم **تطبيقها كلها في نفس التعديل الواحد**.
- **السبب:** صاحب الموقع لا يريد الاضطرار لمراجعة نفس الملف مرات متعددة (مرة للبند 5.1، ثم لاحقاً مرة أخرى للبند 3.3، ثم مرة ثالثة للبند 7.3...). كل ملف يجب أن يُغلق تماماً (بكل ما يخصه من بنود الخطة) في زيارة تعديل واحدة له.
- هذا **لا يلغي** قاعدة "ملف واحد في كل مرة، ثم توقف وانتظر اكملي" أعلاه — بل يُطبَّق **معها**: التوقف يبقى بعد كل *ملف*، لكن كل ملف يُعدَّل مرة واحدة فقط وبكل ما يخصه دفعة واحدة، لا حسب ترتيب ظهور البنود في الخطة.
- ✅ **حالة `sajjad.html`: مكتمل بالكامل** (كل البنود التي تخصه من الخطة طُبِّقت في زيارتين مجمَّعتين: 5.1 أولاً، ثم 3.3/5.3/6.4/7.3/7.4 لاحقاً بعد توفر المعلومات الحقيقية من صاحب الموقع). لا حاجة للرجوع لهذا الملف مرة أخرى إلا إذا تغيّرت حقائق العمل (سنة التأسيس، عدد المشاريع، مدد التنفيذ) مستقبلاً — راجع القسم 3 أدناه للتفاصيل الكاملة.

---

## ✅ ما تم تنفيذه فعلياً حتى الآن

### 1. مراجعة تعديلات أولية من Perplexity Pro (تم التحقق منها وتأكيد صحتها)
تم فحص 4 ملفات عدّلها Perplexity Pro مقارنة بالنسخة الأصلية للموقع، وتبيّن أنها **صحيحة تقنياً بالكامل**، لكنها **لم تتبع قاعدة "ملف واحد في كل مرة"** ولا **ترتيب التنفيذ** المحدد في الخطة (عدّل 4 ملفات دفعة واحدة، وقفز لبنود متأخرة في الترتيب متجاوزاً البند الأهم رقم 1):

| الملف | التعديل | يطابق أي بند في الخطة |
|---|---|---|
| `sitemap.xml` | إضافة سطر `reviews.html` | البند 3.1 ✅ |
| `logo.html` | إضافة `<meta name="robots" content="noindex, nofollow">` | البند 3.6 ✅ |
| `watermark.html` | إضافة نفس سطر `noindex, nofollow` | البند 3.6 ✅ |
| `index.html` | تقصير `<title>` من 90 حرفاً إلى 55 حرفاً | البند 3.4 ✅ |

هذه التعديلات الأربعة **مُدمجة في هذا الملف المضغوط** (النسخ الحالية من الملفات الأربعة أعلاه في هذه الحزمة تحتوي عليها).

### 2. تنفيذ البند 5.1 (الأولوية المطلقة في ترتيب التنفيذ، القسم 8، البند رقم 1) — على `sajjad.html` فقط
**المشكلة الأصلية:** قائمة المنتجات في `sajjad.html` كانت تُبنى بالكامل عبر JavaScript (`fetch('/products.json')` + `innerHTML`) بعد تحميل الصفحة، فلا يراها أي بوت لا ينفّذ JS (`GPTBot`, `ChatGPT-User`, `PerplexityBot`, `ClaudeBot`، وأيضاً Googlebot يعطيها أولوية أقل).

**ما تم تعديله بالضبط في `sajjad.html`:**
- الـ16 منتج من قسم `sajjad` في `products.json` أصبحوا مكتوبين كـ HTML ثابت مباشرة داخل `<div class="products-grid" id="products-grid">` (بدل عبارة "جاري التحميل...")، بنفس تنسيق البطاقة الأصلي (صورة + اسم + وصف + Schema.org microdata).
- سكيما `ItemList`/`Product` (لكل الـ16 منتج) أصبحت أيضاً مكتوبة **ثابتة** داخل `<div id="schema-container">` بدل الاعتماد فقط على حقنها بجافاسكربت.
- **لم يتم حذف أو تغيير الجافاسكربت الأصلي** (`loadProducts()`, `fetch`, دعم تحديثات لوحة التحكم عبر `localStorage`) — بقي كما هو تماماً ليستمر بتحديث المحتوى تلقائياً مستقبلاً (Progressive Enhancement فوق محتوى موجود أصلاً في HTML، وليس بديلاً عنه).
- تعديل تقني بسيط واحد فقط في الجافاسكربت: `document.getElementById('schema-container').appendChild(s)` → `.replaceChildren(s)` — لمنع تكرار سكيما الـ`ItemList` مرتين في الصفحة عند تحديث JS للمحتوى لاحقاً (لأن الآن يوجد سكريبت schema ثابت أصلاً في الصفحة قبل تشغيل JS).
- لم يتم تغيير أي شيء آخر: التصميم، CSS، قسم الأسئلة الشائعة، أزرار الاتصال/واتساب، الـheader/footer — كلها كما هي.

**المصدر المستخدم للبيانات الثابتة:** `products.json` (قسم `sajjad`) كما هو في وقت التنفيذ. إذا تغيّرت المنتجات لاحقاً في `products.json`، يجب إعادة توليد الـHTML الثابت في هذه الصفحة (وليس الاعتماد على JS وحده لتغطية الفرق طويل المدى بالفهرسة).

---

### 3. تنفيذ باقي البنود التي تخص `sajjad.html` دفعة واحدة (تطبيقاً للقاعدة الجديدة أعلاه)
بعد تزويد صاحب الموقع بالحقائق الحقيقية التالية، طُبِّقت البنود المتبقية على `sajjad.html` في زيارة واحدة:

**المعلومات الحقيقية المستخدَمة (يجب استخدام نفس الأرقام في باقي الصفحات لضمان الاتساق):**
- سنة التأسيس: 2016
- عدد المشاريع المنجزة: أكثر من 790
- مدة تركيب السجاد/الموكيت: يوم واحد
- مدة تفصيل المساند: 3 أيام
- مدة تركيب مجالس الديكور الخشبية: 15 يوماً
- نقطة التمايز الحقيقية: أنواع سجاد حصرية غير متوفرة لدى المنافسين، مضغوطة بجودة عالية وخام من مصانع بورصة التركية
- خطوات التركيب الفعلية: تواصل ← زيارة مندوب لأخذ المقاسات ← عرض العينات (النوع/السماكة/الخام) ← الاتفاق على مدة الإنجاز ← التركيب والتنفيذ عبر ورشة مختصة
- الأسعار: طُلب تركها فارغة حالياً (لم تُحدَّث)، تبقى كما هي في `products.json` دون تعديل

**ما تم إضافته في `sajjad.html`:**
- قسم "لماذا سالفورد للسجاد والموكيت؟" (3.3 + 5.3): فقرة تعريفية بصيغة إجابة مباشرة تتضمن سنة التأسيس وعدد المشاريع ونقطة التمايز الحقيقية، + جدول مدد تنفيذ لكل خدمة.
- شريط إحصائيات ثقة أسفل شريط التقييمات (6.4): سنة التأسيس، عدد المشاريع، مدة تركيب السجاد.
- قسم "كيف تتم عملية التركيب" (تحضير لـ7.3): قائمة مرقّمة بالخطوات الفعلية الخمس.
- سكيما `HowTo` جديدة في `<head>` (7.3) بنفس الخطوات الخمس.
- **7.4 (Breadcrumb):** لم يحتج تعديلاً — كان موجوداً بالفعل في الصفحة من قبل.
- لم تُمس الأسعار في `products.json` ولا الأسئلة الشائعة الموجودة أصلاً — فقط أُضيف محتوى جديد.

---

### 4. ✅ `majalis.html` — مكتمل بالكامل (نفس أسلوب `sajjad.html`، بكل البنود دفعة واحدة)
**البند 5.1:** الـ12 منتج من قسم `majalis` في `products.json` أصبحوا HTML ثابتاً في `products-grid` (بدل "جاري التحميل...")، مع سكيما `ItemList`/`Product` ثابتة أيضاً، ونفس تصحيح `appendChild → replaceChildren` لمنع تكرار السكيما.

**البنود 3.3 + 5.3 + 6.4:** أُضيف قسم "لماذا سالفورد لتفصيل المجالس والمساند؟" بصيغة إجابة مباشرة (تأسيس 2016، +790 مشروع)، + جدول مدد تنفيذ (مساند 3 أيام، مجالس ديكور خشبية 15 يوماً)، + شريط إحصائيات ثقة أسفل شريط التقييمات.

**البند 7.3:** أُضيفت سكيما `HowTo` جديدة في `<head>` (5 خطوات: تواصل ← زيارة مندوب لأخذ المقاسات ← اختيار التصميم (قماش/خشب) ← الاتفاق على المدة ← التنفيذ والتسليم)، مع قسم HTML مطابق مرئي للزوار.

**البند 7.4:** كان موجوداً مسبقاً، لم يحتج تعديل.

**ملاحظة تمايز (بند 3.10):** صياغة قسم "لماذا سالفورد" وقسم الخطوات هنا **مختلفة نصياً** عن نفس الأقسام في `sajjad.html` (ليست نفس الجمل بتغيير كلمة فقط)، لتفادي تصنيف الصفحات كمحتوى متطابق.

**الأرقام المستخدمة:** نفس الحقائق الموحّدة المذكورة في قسم "3" أعلاه (2016، +790 مشروع، مدد التنفيذ الثلاث) — لم تُغيَّر ولم تُضَف أرقام جديدة.

---

### 5. ✅ `athath.html` — مكتمل بالكامل (نفس الأسلوب، بصياغة مختلفة نصياً عن الصفحتين السابقتين)
**البند 5.1:** الـ9 منتجات من قسم `athath` في `products.json` أصبحوا HTML ثابتاً في `products-grid` + سكيما `ItemList`/`Product` ثابتة + تصحيح `appendChild → replaceChildren`.

**البنود 3.3 + 5.3 + 6.4:** قسم "لماذا سالفورد للأثاث والديكور الداخلي؟" (تأسيس 2016، +790 مشروع، تفصيل جديد أو تنجيد أثاث قديم) + جدول مدد (مساند 3 أيام، ديكور خشبي 15 يوماً، الكنب/التنجيد "حسب حجم القطعة" لأنه لا يوجد رقم محدد من صاحب الموقع لهذا البند تحديداً — **لم يُخترَع رقم**) + شريط إحصائيات ثقة.

**البند 7.3:** سكيما `HowTo` مخصصة لهذه الصفحة (تفصيل جديد **أو** تنجيد قطعة قديمة — خطوة أولى مختلفة عن باقي الصفحات لأنها تسأل عن نوع الخدمة أولاً).

**البند 7.4:** كان موجوداً مسبقاً.

**ملاحظة مهمة:** لا يوجد رقم دقيق من صاحب الموقع لمدة تفصيل/تنجيد الكنب تحديداً (فقط للسجاد/المساند/المجالس الخشبية) — تُرك هذا كـ"حسب حجم القطعة" بدل اختراع رقم. **إذا زوّد صاحب الموقع لاحقاً برقم دقيق لمدة الكنب/التنجيد، يجب تحديث هذا الجدول في `athath.html` وأيضاً في `ai-handoff-instructions.md` (قسم الحقائق الموحّدة أعلاه).**

---

### 6. ✅ `works.html` — مكتمل بالكامل (آخر ملف في البند 1/5.1، بأسلوب مختلف بنيوياً عن الثلاث صفحات السابقة)
**فحص أولي (كما طُلب في التسليم السابق):** `works.html` يعتمد على `posts.json` (27 منشوراً وقت التنفيذ)، وحقول كل منشور هي `id/title/desc/img/date` (وبعض المنشورات الأحدث تحتوي أيضاً حقول إضافية غير مستخدمة حالياً في الموقع: `images` [مصفوفة صور متعددة]، `video`، `price`، `unit` — **لم تُستخدم هذه الحقول الإضافية** لأن استخدامها (معرض صور متعدد لكل مشروع، فيديو، سعر) توسيع فعلي لميزات الصفحة وليس ضمن نطاق بند 5.1، ويحتاج قراراً منفصلاً من صاحب الموقع لاحقاً).

**البند 5.1:** الـ27 مشروعاً من `posts.json` أصبحوا HTML ثابتاً داخل `#posts-grid` (بدل "جاري التحميل...")، بنفس ترتيب الأحدث أولاً (مطابق لمنطق `[...posts].reverse()` في الجافاسكربت الأصلي). حقل التاريخ في النسخة الثابتة يعرض **تاريخاً مطلقاً مكتوباً بالعربي** (مثل "4 أغسطس 2026") بدل الصيغة النسبية ("اليوم"/"أمس") المستخدمة في الجافاسكربت — لأن الصيغة النسبية تصبح **معلومة خاطئة تلقائياً** بعد أيام إذا بقيت ثابتة في HTML (مثلاً "اليوم" ستبقى مكتوبة للأبد لبوت لا ينفّذ JS). الجافاسكربت نفسه ما زال يعرض التاريخ النسبي فور تحميله في متصفح الزائر (Progressive Enhancement فوق التاريخ المطلق الثابت).

سكيما `ItemList`/`ImageObject` (للـ27 مشروعاً) أصبحت أيضاً ثابتة داخل `<div id="schema-container">`. نفس تصحيح `appendChild → replaceChildren` المطبَّق في الصفحات الثلاث السابقة طُبِّق هنا أيضاً (الجافاسكربت يستهدف الآن `#schema-container` بدل `document.head` مباشرة، لمنع تكرار السكيما عند تحديث المحتوى من لوحة التحكم).

**إصلاح خطأ تقني موجود مسبقاً في الملف (غير مرتبط ببند من الخطة):** النسخة الأصلية من `works.html` كانت تحتوي **كل محتوى نهاية الصفحة مكرراً مرتين بالكامل** (قسم `sticky` + `<script>` بأكمله)، مع بقية ذلك التكرار **بعد وسم `</html>` الأول** (كود ميت لا يعمل لكنه يُثقل حجم الملف ويعطي انطباعاً سيئاً لأي مراجعة يدوية للكود لاحقاً). تم حذف هذا التكرار بالكامل والإبقاء على نسخة واحدة نظيفة فقط.

**البنود 3.3 + 5.3:** أُضيفت فقرة تعريفية بصيغة إجابة مباشرة (قسم `intro-section`) توضح أن الصور حقيقية ومنفذة فعلياً وليست من الإنترنت، مع سنة التأسيس (2016) وعدد المشاريع (+790).

**البند 6.4:** شريط إحصائيات ثقة (`stats-bar`): سنة التأسيس، عدد المشاريع المنجزة، وعدد المشاريع المعروضة في هذه الصفحة تحديداً (27) — إحصائية فريدة لهذه الصفحة فقط غير موجودة في الصفحات الثلاث الأخرى.

**البند 7.3 (بصياغة مختلفة عن الصفحات الثلاث الأخرى):** بما أن `works.html` صفحة معرض تجمع مشاريع من كل الخدمات (وليست صفحة خدمة واحدة كالسجاد أو المجالس)، لم يكن مناسباً تكرار نفس "خطوات التركيب" الموجودة في الصفحات الأخرى. بدلاً من ذلك أُضيف قسم/سكيما `HowTo` مختلف كلياً في الموضوع: **"كيف نوثّق مشاريعنا"** — خطوات من استقبال الطلب حتى توثيق التسليم بالصور (تنتهي فعلياً بربط منطقي مع الصور المعروضة في نفس الصفحة).

**قسم جديد لم يكن موجوداً في الصفحات الأخرى بنفس الشكل:** أسئلة شائعة (`faq-section`) خاصة بصفحة الأعمال تحديداً + سكيما `FAQPage` جديدة في `<head>` — أسئلة عن مصداقية الصور، تكرار التحديث، وإمكانية طلب تصميم مشابه لمشروع من المعرض (أسئلة مختلفة كلياً عن أسئلة صفحات الخدمة، تفادياً للتكرار حسب بند 3.10).

**البند 7.4 (Breadcrumb):** لم يحتج تعديلاً — كان موجوداً بالفعل في الصفحة من قبل.

**ملاحظة إضافية (تحسين UX بسيط غير مطلوب صراحة في الخطة):** عنصر "⏳ جاري التحميل..." أصبح مخفياً افتراضياً (`display:none`) بدل ظاهراً دائماً عند فتح الصفحة، لأن المحتوى أصبح موجوداً فعلياً في HTML من البداية ولا داعي لإظهار رسالة تحميل فوق محتوى ظاهر أصلاً.

---

## ✅ البند 1 (5.1) اكتمل بالكامل الآن على الملفات الأربعة (`sajjad.html`, `majalis.html`, `athath.html`, `works.html`)

### 7. البند 3 (6.3 + 6.1) — بدأ التنفيذ، على `index.html` فقط حتى الآن
**فحص أولي قبل التنفيذ (كما هو مطلوب):**
- **6.3 (زر واتساب عائم + اتصال سريع):** تبيّن أنه **موجود بالفعل مسبقاً في كل الصفحات الست** (`index.html`, `sajjad.html`, `majalis.html`, `athath.html`, `reviews.html`, `works.html`) — شريط `sticky` أسفل الشاشة بزري واتساب واتصال. **لا حاجة لأي تعديل على هذا الجزء.**
- **6.1 (GA4 + GTM):** لم يكن موجوداً إطلاقاً (الموجود سابقاً فقط Google Ads `AW-6824246360` للتحويلات، وهذا مختلف عن GA4/GTM). طُلب من صاحب الموقع معرّف GA4 حقيقي (لم يُخترَع أي رقم)، وزوّد بـ: **`G-8WRQFKRYXP`**.

**ما تم تعديله بالضبط في `index.html`:**
- إضافة `gtag('config','G-8WRQFKRYXP');` داخل سكربت gtag الموجود أصلاً (بدون تحميل ملف `gtag.js` مرتين — نفس السكربت يخدم Google Ads وGA4 معاً عبر نفس الـ dataLayer).
- إضافة تتبع أحداث GA4 مخصصة (`gtag('event', ...)`) على كل نقاط التواصل في الصفحة: زر واتساب الرئيسي، زر الاتصال الرئيسي (بالإضافة لتتبع تحويل Google Ads الموجود مسبقاً، دون حذفه)، رقم الهاتف في شارة الهيدر، وروابط الهاتف/واتساب في الفوتر، وزري واتساب/اتصال في الشريط اللاصق. الأحداث: `phone_click` و`whatsapp_click`.
- لا يوجد `<form>` في `index.html` حالياً، فلم يُضَف تتبع نماذج لهذه الصفحة (لا يوجد ما يُتتبَّع).
- لم يُمَس أي شيء آخر في الصفحة (التصميم، Google Ads، المحتوى).

**GTM (Google Tag Manager):** لم يُركَّب بعد — صاحب الموقع اختار البدء بـ GA4 وحده حالياً، مع إمكانية إضافة GTM لاحقاً كخطوة منفصلة إذا رغب.

**⚠️ ملاحظة مهمة للمنفّذ التالي:** معرّف `G-8WRQFKRYXP` يجب استخدامه بنفس الطريقة (نفس سطر `gtag('config',...)` + نفس أحداث `phone_click`/`whatsapp_click`) في باقي الصفحات الخمس المتبقية: `sajjad.html`, `majalis.html`, `athath.html`, `reviews.html`, `works.html` — **ملف واحد في كل مرة**، ثم التوقف وانتظار "اكملي"، تماماً كما جرى مع `index.html`.

### 8. `sajjad.html` و`majalis.html` — GA4 مُركَّب (بطلب صريح من صاحب الموقع بتجميع ملفين في هذه الخطوة تحديداً، استثناءً من القاعدة المعتادة)
نفس التعديل بالضبط المطبَّق على `index.html`، بلا أي فروقات في المنطق:
- `gtag('config','G-8WRQFKRYXP');` أُضيف داخل سكربت gtag الموجود أصلاً في كلا الملفين.
- أحداث `whatsapp_click` و`phone_click` أُضيفت على: الزر الرئيسي واتساب، الزر الرئيسي اتصال (فوق تحويل Google Ads الموجود، دون حذفه)، روابط الفوتر، وزرَي الشريط اللاصق.
- لا يوجد شارة هاتف في الهيدر ولا `<form>` في أي من الصفحتين (بعكس `index.html` الذي كان فيه شارة هاتف بالهيدر) — فلم يكن هناك عنصر إضافي للتتبع غير ما ذُكر.
- لم يُمَس أي شيء آخر (التصميم، المحتوى، سكيما المنتجات الثابتة المُنفَّذة سابقاً في البند 1).

---

### 9. `athath.html`, `reviews.html`, `works.html` — GA4 مُركَّب (استكمالاً للبند 3، بطلب صريح من صاحب الموقع بتجميع عدة خطوات في هذه الجلسة)
نفس التعديل بالضبط المطبَّق على `index.html`/`sajjad.html`/`majalis.html`، بلا أي فروقات في المنطق:
- `gtag('config','G-8WRQFKRYXP');` أُضيف داخل سكربت gtag الموجود أصلاً في الملفات الثلاثة.
- أحداث `whatsapp_click` و`phone_click` أُضيفت على كل نقاط التواصل (الزر الرئيسي واتساب/اتصال، روابط الفوتر، زرَي الشريط اللاصق).
- `reviews.html` تحديداً فيها زر واتساب إضافي فريد لهذه الصفحة (إرسال تقييم عبر واتساب) — أُضيف له نفس حدث `whatsapp_click` أيضاً.
- لا شارة هاتف بالهيدر ولا `<form>` في أي من الملفات الثلاثة.
- لم يُمَس أي شيء آخر.

## ✅ البند 3 (6.3 + 6.1) اكتمل بالكامل الآن على الصفحات الست (`index`, `sajjad`, `majalis`, `athath`, `reviews`, `works`)

### 10. `about.html` — إنشاء جديد (البند 4: 7.1 + 5.2)
**ملف جديد بالكامل** يطبّق البند 7.1 (صفحة "عن سالفورد" الكاملة) والبند 5.2 (توحيد NAP وإشارات الكيان):
- محتوى: من نحن (بنفس الحقائق الموحّدة: تأسيس 2016، +790 مشروع، نقطة التمايز الحقيقية لأنواع السجاد الحصرية من مصانع بورصة التركية)، قائمة الخدمات مع روابط داخلية لصفحات `sajjad.html`/`majalis.html`/`athath.html`، صور فعلية من نفس مجموعة صور `index.html`، بطاقة معلومات تواصل (العنوان الكامل، ساعات العمل، الهاتف، واتساب).
- **العنوان وساعات العمل والهاتف مطابقة تماماً** لما هو موجود في schema `index.html` (نفس النص بالحرف: "الضجيج، محافظة الفروانية، الكويت" و"10 صباحاً حتى 9 مساءً").
- سكيما `LocalBusiness`/`FurnitureStore` بنفس `@id": "https://salfordkw.shop/#business"` الموجود في `index.html` (لتوحيد الكيان بدل تكراره ككيان منفصل) + `foundingDate: 2016` + نفس `sameAs` الموجود حالياً في `index.html`.
- سكيما `BreadcrumbList` جديدة (الرئيسية > عن سالفورد).
- GA4 + أحداث `phone_click`/`whatsapp_click` مُركَّبة من البداية بنفس نمط باقي الصفحات (لا حاجة لزيارة لاحقة لهذا الملف من أجل التتبع).

**✅ `sameAs` مكتمل الآن — روابط حقيقية زوّد بها صاحب الموقع:**
- إنستقرام: `https://www.instagram.com/salforad/`
- تيك توك: `https://www.tiktok.com/@salford_kuwait`

أُضيف الرابطان إلى `sameAs` في سكيما `LocalBusiness` في **كل من** `index.html` و`about.html` معاً (لتوحيد الكيان)، وأُضيف أيضاً صف "📱 تابعونا" مرئي للزوار في بطاقة معلومات `about.html`.

**✅ الربط الداخلي (Internal Linking) — تم:**
أُضيف سطر موحّد في فوتر **كل الصفحات الست** (`index.html`, `sajjad.html`, `majalis.html`, `athath.html`, `reviews.html`, `works.html`):
`🔗 من نحن | إنستقرام | تيك توك` — رابط "من نحن" يشير لـ `/about.html`، ورابطا إنستقرام وتيك توك يفتحان بـ `target="_blank"`. `about.html` لم تعد صفحة يتيمة.

**✅ `sitemap.xml` — تم:**
أُضيف سطر لـ `about.html` بنفس نمط باقي الصفحات (`priority 0.8`، `changefreq monthly`).

## ✅ البند 4 (7.1 + 5.2) اكتمل بالكامل الآن

---

### 11. ✅ البند 5 (7.2) — صفحات منتجات مستقلة بـ Product schema — مكتمل
**ملف جديد بالكامل لكل منتج حقيقي في `products.json`** داخل مجلد جديد `/catalog/` (اسم مختلف عن مجلد `/products/` الحالي المخصص للصور، لتفادي التعارض):
- **37 صفحة** أُنشئت بنمط اسم `catalog/{القسم}-{الرقم بترتيبه في products.json}.html` (مثال: `catalog/sajjad-01.html` = "سجاد تركي اكادير").
- كل صفحة تحتوي: سكيما `Product` (name/description/image/brand/offers/url) + سكيما `BreadcrumbList` (الرئيسية ← صفحة القسم ← المنتج) + سكيما `LocalBusiness` بنفس `@id` الموحّد + صورة كبيرة + جدول مواصفات (التصنيف، مدة التنفيذ التقريبية، السعر) + شريط إحصائيات ثقة (2016، +790، 4.9★) + فقرة "عن سالفورد" مختصرة + زر رجوع لصفحة القسم + GA4 وأحداث phone_click/whatsapp_click + الفوتر الموحّد (من نحن/إنستقرام/تيك توك).
- **السعر:** يُعرض إن وُجد في `products.json`، وإلا يظهر نص "يُحدَّد عند المعاينة — تواصل معنا" في الجدول (بدون اختراع رقم)، وسكيما `offers` تُترك بدون `price` في هذه الحالة.
- **⚠️ استثناء مهم — 4 صفحات بـ `noindex, follow` مؤقتاً (لا تُفهرَس حتى تتوفر أوصاف حقيقية):**
  بعض المنتجات في `products.json` وصفها النص الافتراضي الحرفي "منتج جديد من سالفورد للأثاث والمفروشات" (وليس وصفاً حقيقياً)، وإنشاء صفحة مفهرسة بهذا النص يخالف "شرط أساسي قبل التنفيذ" في البند 7.2 من الخطة (محتوى كافٍ لكل صفحة) ويخلق محتوى شبه مكرر بين هذه الصفحات. الصفحات التالية أُنشئت (بحيث لا تُصبح روابط معطّلة) لكنها `<meta name="robots" content="noindex, follow">`:
  - `catalog/sajjad-13.html` (سجاد حفر)
  - `catalog/sajjad-16.html` (نقشة حفر يدوي)
  - `catalog/majalis-11.html` (مجلس ديكور الكويت صباح الاحمد)
  - `catalog/majalis-12.html` (مجلس خشب ديكور الصباحية)
  **مطلوب من صاحب الموقع:** تزويدنا بوصف حقيقي مختصر (سطر أو سطرين) لهذه المنتجات الأربعة عبر لوحة التحكم (`admin.html`/`add.html`) أو مباشرة، وعندها يُعاد توليد صفحاتها وتغيير `robots` إلى `index, follow` وإضافتها لـ`sitemap.xml`.
- **الربط الداخلي:** أُضيف رابط "التفاصيل والسعر ←" (`itemprop="url"`) داخل كل بطاقة منتج في `sajjad.html`/`majalis.html`/`athath.html` (في الـHTML الثابت وفي دالة `renderProducts()` الخاصة بالجافاسكربت معاً، بنفس منطق الترقيم `{قسم}-{رقم}`)، وأُضيف حقل `"url"` لكل عنصر `Product` داخل سكيما `ItemList` في الصفحات الثلاث. هذا يعني أن صفحات المنتج الجديدة **ليست يتيمة** — يصل إليها الزائر والبوت من صفحة القسم مباشرة، وليس فقط عبر sitemap.
- **`sitemap.xml`:** أُضيفت الصفحات الـ33 القابلة للفهرسة (37 - 4 مستثناة) بـ`priority 0.6`. الصفحات الأربع المستثناة **لم تُضَف** لـsitemap (منطقي طالما `noindex`).
- **لم تُنشأ صفحات "Brand" منفصلة** (كما أوصت الخطة) — صفحة منتج واحدة لكل تشكيلة تكفي.
- **مصدر الملفات المساعدة (ليست جزءاً من الموقع، أُبقيت خارج الحزمة النهائية للموقع):** `gen_products.py` (سكربت التوليد) و`catalog_index_list.json` — إذا احتاج منفّذ لاحق إعادة توليد الصفحات (مثلاً بعد تحديث الأوصاف الأربعة الناقصة)، يمكن تشغيل نفس السكربت بعد تحديث `products.json`.

### 12. ✅ البند 6 (تقصير العنوان + `loading="lazy"`) — فُحص، لا حاجة لأي تعديل
- **تقصير عنوان `index.html`:** تم فعلاً مسبقاً (55 حرفاً، تم التأكد بالعدّ الفعلي للأحرف).
- **`loading="lazy"` (3.5):** بالفحص الفعلي، `index.html` لا يحتوي إلا على صورة واحدة تُبنى ديناميكياً عبر JS (شبكة "أعمالنا")، وهي مطبَّقة بالفعل بشكل صحيح: أول صورة `loading="eager"` (لتحسين LCP) والباقي `loading="lazy"` — لا توجد صور أخرى بالصفحة (باقي العناصر خلفيات CSS لا تحتاج `loading`). **لم يُعدَّل أي شيء في `index.html`** لأنه مطابق للمطلوب فعلاً.

## ✅ البند 5 (7.2) والبند 6 اكتملا الآن

### 13. `add.html` — ضغط تلقائي للصور عند الرفع (طلب صريح من صاحب الموقع، خارج ترتيب القسم 8 لكنه يخدم البند 7 القادم)
**الطلب:** أي صورة تُرفع من جهاز صاحب الموقع عبر `add.html` تُضغط تلقائياً قبل رفعها لمستودع GitHub.
**ما تم تعديله بالضبط في `add.html`:**
- دالة جديدة `compressImage()` تُصغّر أي صورة مختارة عبر `canvas` بحيث لا يتجاوز أطول بُعد فيها 1600px، وتُحوّلها WebP بجودة 0.8 (أو JPEG تلقائياً كبديل في المتصفحات القديمة التي لا تصدّر WebP).
- `handleImageSelect()` أصبحت `async` وتستدعي `compressImage()` على كل صورة قبل إضافتها لقائمة `selectedImages` — أي أن الملف الذي يُرفَع فعلياً لـGitHub (وبالتالي الرابط النهائي للمنتج) هو النسخة المضغوطة، وليس الأصل.
- في حال فشل الضغط لأي سبب (متصفح لا يدعم canvas مثلاً)، تُستخدم الصورة الأصلية كما هي بدل فقدانها بالكامل (Fallback آمن).
- تحديث نص الإرشاد في واجهة الرفع ليوضح أن الصور "تُضغط تلقائياً قبل الرفع".
- لم يُمَس منطق الفيديو، ولا آلية رفع GitHub نفسها (`uploadFileToGitHub`)، ولا حد الحجم الأقصى (6MB للصورة الواحدة قبل الضغط).
**ملاحظة للمنفّذ التالي:** هذا يحل مشكلة الصور الثقيلة **للمنتجات الجديدة فقط من الآن فصاعداً**. الصور القديمة الموجودة أصلاً (المستضافة جزئياً على `i.ibb.co` وجزئياً داخل `/products/` و`/works/` في المستودع) **لم تُضغط بهذا التعديل** — ضغطها يبقى ضمن البند 7 (3.2 + 6.6 + 7.5) اللاحق في ترتيب التنفيذ.

---

### 14. ✅ البند 7 (3.2 + 6.6 + 7.5) — ضغط الصور + WebP + أسماء وصفية + Image Sitemap — مكتمل (بالحدود الممكنة)

**⚠️ اكتشاف مهم أثرّ على نطاق التنفيذ:** بالفحص الفعلي لـ `products.json`، تبيّن أن **أغلب صور المنتجات (16 من 16 لسجاد جزئياً، وأغلب مجالس/أثاث) مستضافة خارجياً على `i.ibb.co`** وليست ملفات داخل مستودع الموقع (`/products/`, `/works/`). لا يمكن ضغط أو إعادة تسمية صور مستضافة على خدمة خارجية لا نملك التحكم بملفاتها — هذا تعديل خارج نطاق ما يمكن تنفيذه على ملفات الموقع، ويحتاج قراراً من صاحب الموقع لاحقاً (تحميل هذه الصور فعلياً إلى مجلد `/products/` أو `/works/` في المستودع لتصبح قابلة للتحسين). **لم تُعدَّل أي صورة على `i.ibb.co`.**

**ما تم تنفيذه فعلياً — فقط على الصور المستضافة محلياً في `/products/` و`/works/` (35 صورة):**
- تحويل جميع الصور من JPG/PNG إلى **WebP** (جودة 82، وتخفيض أبعاد الصور التي يتجاوز أطول بُعد فيها 1600px — أو 1200px تحديداً لصورتين كانتا لا تزالان ثقيلة جداً عند 1600px بجودة 82، فتم تخفيضهما لجودة 68 عند 1200px).
- **إعادة تسمية كل صورة باسم وصفي بالعربي** مبني على اسم المنتج (`code`) في `products.json` أو عنوان المشروع (`title`) في `posts.json`، بصيغة `{الاسم}-سالفورد.webp` (مع ترقيم `-2`, `-3`... عند تكرار نفس الاسم لصور متعددة لنفس المنتج). مثال: `product_1785878837328.jpg` → `سجاد-حفر-سالفورد.webp`.
- **تحديث كل مرجع** لهذه الأسماء القديمة في: `products.json`, `posts.json`, `sajjad.html`, `majalis.html`, `works.html` (النسخة الثابتة HTML وسكيما `ItemList` معاً)، وكل صفحات `catalog/*.html` التي كانت تستخدم صوراً محلية (`sajjad-13/14/15/16`, `majalis-07/08/09/10/11/12`). لم يُترك أي مرجع معطّل — تم التأكد بالبحث الشامل في كل الملفات بعد التعديل.
- **صورتان "يتيمتان" غير مستخدمتين في أي صفحة أو JSON حالياً** (`product_1785694712597.jpg`, `work_1777737343365.jpg`) — تم ضغطهما وتحويلهما أيضاً (باسم عام `...-orphan.webp` لعدم وجود اسم منتج/مشروع مرتبط بهما)، لكن **لم يُقرَّر شيء بشأن حذفهما** — قرار يخص صاحب الموقع، خارج نطاق هذا البند.
- **النتيجة الإجمالية:** حجم الصور المحلية الـ35 انخفض من ~12.3 ميجابايت إلى ~5.5 ميجابايت (تخفيض ~56%)، وكل صورة أصبحت ضمن أو قريبة من الهدف الموصى به في الخطة (150-300KB للصور الفعلية، أقل لما هو أصغر أصلاً).
- **ملفات الفيديو (`video_...mp4`) لم تُمَس** — خارج نطاق هذا البند (البند 3.2 يخص الصور فقط).
- **`alt` النصوص:** بالفحص الفعلي، كانت غالبية الصفحات تحتوي أصلاً `alt` وصفياً معقولاً (اسم المنتج + الموقع)، بعضها فقط بصيغة عامة مثل "كود 05" — وهذا لأن اسم المنتج نفسه في `products.json` عام وليس خطأً في القالب. **لم يُخترَع وصف أدق** لهذه الحالات لعدم توفر معلومة حقيقية أدق من صاحب الموقع (نفس منطق البند 7.2 الخاص بالمنتجات الأربعة `noindex`).
- **Image Sitemap:** أُضيف `xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"` لعنصر `<urlset>` في `sitemap.xml`، وأُضيفت وسوم `<image:image><image:loc>...</image:loc></image:image>` لكل صورة حقيقية داخل: `sajjad.html`, `majalis.html`, `athath.html` (صورة كل منتج معروض بها)، `works.html` (صور المشاريع الـ27)، وكل صفحة `catalog/*.html` المفهرسة (صورتها الوحيدة) — شاملة الصور الخارجية على `i.ibb.co` والصور المحلية الجديدة معاً (الـImage Sitemap لا يشترط أن تكون الصور من نفس الدومين). لم تُضَف صور للصفحات الأربع `noindex` (منطقي، هي خارج sitemap أصلاً).
- **`lastmod`:** حُدِّث لتاريخ اليوم لكل الصفحات التي تغيّر محتواها الفعلي بهذا التعديل (`sajjad.html`, `majalis.html`, `athath.html`, `works.html`، وصفحات catalog المتأثرة بتغيير مسار صورها).

**⚠️ مطلوب من صاحب الموقع (قرار مستقبلي، ليس عائقاً للنشر الآن):**
هل يرغب بترحيل الصور المستضافة على `i.ibb.co` لتصبح ملفات محلية داخل مستودع الموقع؟ إن وافق، يمكن تحميلها وضغطها وتسميتها بنفس الأسلوب المطبَّق هنا على الصور المحلية.

---

### 15. ✅ البند 8 (3.3 + 5.3 + 6.4) — توسيع المحتوى النصي لباقي الصفحات — مكتمل

**فحص أولي:** الصفحة الوحيدة المتبقية التي كانت لا تزال ضمن حدود "Thin Content" المذكورة في البند 3.3 (بعد أن أُنجزت `sajjad.html`/`majalis.html`/`athath.html`/`works.html` سابقاً، وبعد أن `index.html` مستثناة أصلاً لأنها الأفضل أداءً) هي **`reviews.html`** (213 كلمة فعلية بالفحص المباشر، مطابق لتقدير الخطة ~205).

**ما تم تعديله بالضبط في `reviews.html`:**
- فقرة تعريفية بصيغة "إجابة مباشرة" (5.3) أسفل ملخص التقييم: توضح أن التقييمات حقيقية من عملاء فعليين وليست مصطنعة، مع ذكر سنة التأسيس (2016) وعدد المشاريع (+790) — نفس الحقائق الموحّدة المستخدمة في باقي الصفحات.
- شريط إحصائيات ثقة (6.4): سنة التأسيس، عدد المشاريع، تقييم 4.9★.
- قسم "أسئلة شائعة عن تقييماتنا" (3 أسئلة **مختلفة كلياً** عن أسئلة صفحات الخدمة الأخرى — عن مصداقية التقييمات، كيفية إضافة تقييم، والربط بصور المشاريع الفعلية في `works.html` — تفادياً لتكرار المحتوى حسب البند 3.10)، مع سكيما `FAQPage` جديدة في `<head>` (منفصلة عن سكيما `Review`/`AggregateRating` الموجودة أصلاً).
- لم يُمَس: قسم التقييمات نفسه، زر "أرسل تقييمك"، أزرار التواصل، الفوتر، أو أي تصميم/CSS خارج الأنماط الجديدة المطلوبة للأقسام المضافة.
- **النتيجة:** عدد الكلمات الفعلي ارتفع من 213 إلى 360 كلمة (تم التأكد بعدّ فعلي للنص المعروض، بعد استثناء الأكواد والأنماط).
- `sitemap.xml`: تم تحديث `lastmod` لـ `reviews.html` إلى تاريخ اليوم.

**✅ البند 8 (3.3 + 5.3 + 6.4) مكتمل الآن على كل صفحات الموقع** (كان قد أُنجز سابقاً لـ sajjad/majalis/athath/works، و`about.html` وصفحات catalog تضمّنت محتوى كافياً منذ إنشائها في البندين 4 و5، و`index.html` مستثناة لأنها الأفضل أداءً أصلاً بلا مشكلة محتوى قليل).

---

### 16. ✅ البند 9 (7.3 + 7.4) — HowTo schema + تعميم Breadcrumb — مكتمل

**فحص أولي شامل (كما هو مطلوب قبل أي تعديل):** تم فحص كل الصفحات (`index`, `sajjad`, `majalis`, `athath`, `works`, `reviews`, `about`, وكل صفحات `catalog/*.html`) بحثاً عن `BreadcrumbList` و`HowTo`:

| الصفحة | Breadcrumb | HowTo |
|---|---|---|
| `index.html` | ✅ موجود مسبقاً | ❌ لم يكن موجوداً |
| `sajjad.html`, `majalis.html`, `athath.html`, `works.html` | ✅ موجود مسبقاً | ✅ موجود مسبقاً (من بنود سابقة) |
| `reviews.html`, `about.html` | ✅ موجود مسبقاً | لا ينطبق (لا توجد عملية "كيف" منطقية بهاتين الصفحتين) |
| كل صفحات `catalog/*.html` (37 صفحة) | ✅ موجود مسبقاً (أُضيف في البند 5/7.2) | لا ينطبق (تكرار HowTo لكل منتج فردي كان سيخلق محتوى شبه مكرر عبر 37 صفحة — نفس منطق البند 7.7 من الخطة) |

**النتيجة:** ✅ **`BreadcrumbList` مكتمل فعلياً على كل صفحة في الموقع بلا استثناء** — لم يحتج أي تعديل، فقط تأكيد.

**التعديل الوحيد المطلوب فعلياً — `index.html` فقط:**
- الصفحة الرئيسية كانت الصفحة الوحيدة من الصفحات "العامة" (غير صفحات خدمة فردية) التي لا تحتوي `HowTo`. أُضيف:
  - قسم مرئي جديد "كيف تطلب من سالفورد؟" (5 خطوات مرقّمة) بعد قسم "نخدم جميع مناطق الكويت" وقبل الفوتر — بصيغة عامة تغطي كل الخدمات (بخلاف HowTo كل صفحة خدمة الذي يخص خطواتها التفصيلية فقط).
  - سكيما `HowTo` جديدة في `<head>` بنفس الخطوات الخمس (نفس الأسلوب المستخدم في باقي الصفحات: قسم مرئي + سكيما مطابقة).
- لم يُمَس أي شيء آخر في `index.html` (التصميم، الأقسام الموجودة، السكيمات الأخرى).
- تم التحقق من صلاحية كل الـ4 كتل `JSON-LD` في الصفحة بعد التعديل (بدون أخطاء).
- `sitemap.xml`: تحديث `lastmod` للصفحة الرئيسية إلى تاريخ اليوم.

**✅ البند 9 (7.3 + 7.4) مكتمل بالكامل الآن.**

---

### 17. ✅ البند 10 (6.2) — صفحة FAQ عامة مستقلة (`faq.html`) — إنشاء جديد

**ملف جديد بالكامل** `faq.html` يجمع أسئلة **عامة تخص المحل ككل** (وليست خاصة بخدمة معينة — تلك موجودة أصلاً داخل كل صفحة خدمة من البند 8/3.3):
- **6 أسئلة:** طرق الدفع (كاش/تحويل بنكي)، مناطق التغطية (كل الكويت)، مدة التنفيذ العامة (نفس الحقائق الموحّدة: سجاد/موكيت يوم، مساند 3 أيام، مجالس خشبية 15 يوماً، أثاث/تنجيد حسب حجم القطعة)، الضمان (بصياغة عامة غير مُخترَعة — "يختلف حسب الخدمة ويُتفق عليه مع العميل"، لأنه لا توجد سياسة ضمان محددة زوّدنا بها صاحب الموقع)، طريقة التواصل لمعاينة/عرض سعر، ومنذ متى تعمل سالفورد (2016، +790 مشروع).
- فقرة تعريفية بصيغة "إجابة مباشرة" (5.3) + شريط إحصائيات ثقة (6.4) بنفس نمط باقي الصفحات.
- سكيما `FAQPage` + `BreadcrumbList` (الرئيسية > الأسئلة الشائعة) في `<head>`.
- صندوق "ملاحظة" يوجّه لصفحات الخدمة الثلاث لأي سؤال خاص بخدمة معينة (تفادي التكرار، ربط داخلي إضافي).
- GA4 + أحداث `phone_click`/`whatsapp_click` مُركَّبة من البداية (نفس نمط باقي الصفحات).
- تم التحقق من صلاحية كتلتَي `JSON-LD` وتطابق الوسوم (`<div>`) بعد الإنشاء — لا أخطاء.

**الربط الداخلي:** أُضيف رابط "الأسئلة الشائعة" في فوتر **كل الصفحات السبع** (`index.html`, `sajjad.html`, `majalis.html`, `athath.html`, `works.html`, `reviews.html`, `about.html`) بجانب رابط "من نحن" الموجود مسبقاً — بحيث لا تكون `faq.html` صفحة يتيمة.

**`sitemap.xml`:** أُضيف سطر لـ `faq.html` (`priority 0.7`)، وتم تحديث `lastmod` لكل الصفحات السبع التي تغيّر فوترها بهذا التعديل.

**✅ البند 10 (6.2) مكتمل الآن.**

---

### 18. ⚠️ البند 11 (6.5 + 7.6) — مركز المعرفة/المدونة — تم الإنشاء، **بانتظار مراجعة بشرية قبل النشر الفعلي**

**فحص أولي والتزام بشرط الخطة:** البند 11 في الخطة يشترط صراحة "مراجعة بشرية كاملة لأي محتوى يُكتب بمساعدة الذكاء الاصطناعي، وقرار من صاحب الموقع بالمواضيع المطلوب البدء بها فعلياً قبل الكتابة". قبل أي كتابة، عُرضت قائمة المواضيع المقترحة (6.5 + 7.6 مدمجة) على صاحب الموقع، فاختار **كل المواضيع دفعة واحدة**.

**ما تم إنشاؤه فعلياً:**
- **`blog.html`** — الصفحة الرئيسية لمركز المعرفة (بطاقات لكل مقال + صورة + مقتطف)، بسكيما `CollectionPage` + `BreadcrumbList`.
- **مجلد `/blog/` جديد** (بنفس منطق مجلد `/catalog/` السابق) يحتوي **7 مقالات كاملة**:
  1. `blog/choose-carpet.html` — كيف تختار السجاد المناسب لمنزلك
  2. `blog/carpet-vs-mokeet.html` — الفرق بين الموكيت والسجاد
  3. `blog/majlis-colors.html` — أفضل ألوان مجالس الديكور 2026
  4. `blog/clean-carpet.html` — كيفية تنظيف السجاد والحفاظ عليه
  5. `blog/majlis-decor-trends.html` — أحدث ديكورات المجالس بالكويت
  6. `blog/turkish-vs-chinese-carpet.html` — الفرق بين السجاد التركي والصيني (دليل مقارن، بند 7.6)
  7. `blog/best-mokeet-kuwait.html` — دليل اختيار أفضل موكيت في الكويت (دليل مقارن، بند 7.6)
- كل مقال يحتوي: فقرة "إجابة مباشرة" أعلى المقال (نمط GEO)، 4-5 أقسام محتوى فعلي (250-400 كلمة تقريباً لكل مقال)، شريط إحصائيات ثقة (2016 / +790 / 4.9★)، سطر "بقلم فريق سالفورد" (بديل بسيط عن صفحة مؤلف مستقلة، حسب توصية البند 7.12)، روابط داخلية لمواضيع ذات صلة ولصفحة الخدمة المناسبة (`sajjad.html`/`majalis.html`)، سكيما `Article` + `BreadcrumbList` + إشارة `LocalBusiness` بنفس `@id` الموحّد، GA4 وأحداث `phone_click`/`whatsapp_click`، والفوتر الموحّد.
- **لم تُخترَع أي حقيقة أو معلومة جديدة** — كل الأرقام والحقائق (2016، +790، مدد التنفيذ، مصدر الخامة من بورصة) هي نفسها الحقائق الموحّدة المستخدمة في باقي الموقع.
- **الربط الداخلي:** أُضيف رابط "مركز المعرفة" في فوتر **كل الصفحات الثمانية** (`index`, `sajjad`, `majalis`, `athath`, `works`, `reviews`, `about`, `faq`) — `blog.html` وكل المقالات ليست يتيمة.
- **`sitemap.xml`:** أُضيف سطر لـ `blog.html` (`priority 0.7`) و7 أسطر للمقالات (`priority 0.6` لكل مقال)، وتم التحقق من صحة الـXML بعد التعديل.
- تم التحقق برمجياً من صلاحية كل كتل `JSON-LD` في الملفات الـ16 المتأثرة (8 مقالات/صفحة مركز معرفة + 8 صفحات فوتر مُعدَّلة).

**⚠️ لم يُنشر بعد — ينتظر خطوتين من صاحب الموقع قبل الدفع لـ GitHub:**
1. **مراجعة بشرية كاملة لنص كل مقال** (المطلوبة صراحة في نص البند 11) — التأكد أن الأسلوب والمعلومات تعكس فعلاً رأي/خبرة المحل ولا تحتوي أي مبالغة أو معلومة غير دقيقة.
2. تأكيد نهائي بالنشر بعد المراجعة.

**ملف مساعد (ليس جزءاً من الموقع النهائي):** `gen_blog.py` — يمكن تشغيله لاحقاً لتعديل نصوص المقالات أو إضافة مواضيع جديدة من قائمة الخطة دون كتابة HTML يدوياً من الصفر.

### 19. ✅ البند 12 (5.4) — ملف `llms.txt` — مكتمل

**ملف جديد** `llms.txt` في جذر الموقع، بنفس الشكل المقترح في القسم 5.4 من الخطة (تعريف مختصر بالمحل + روابط لأهم الصفحات)، مع إضافة روابط الصفحات الجديدة التي لم تكن موجودة وقت كتابة الخطة الأصلية (`about.html`, `faq.html`, `blog.html`).

---

### 20. ⏸️ البند 13 (6.7) — صفحات استهداف محلي — مؤجَّل بقرار صريح من صاحب الموقع

**السبب:** الخطة (القسم 6.7) تحذّر صراحة من خطر Doorway Pages / Thin Duplicate Content إذا كانت صفحات المناطق تحتوي نفس النص تقريباً مع تغيير اسم المنطقة فقط — وتشترط محتوى **مختلف فعلياً** لكل منطقة (مشاريع حقيقية منفذة فيها، تقييمات عملاء منها).

عند السؤال المباشر، أكّد صاحب الموقع أنه **لا يملك حالياً أي مشاريع أو تقييمات عملاء موثّقة ومرتبطة بمنطقة جغرافية محددة** (لا الضجيج ولا الفروانية ولا غيرهما). بناءً على ذلك، **تقرَّر عدم تنفيذ هذا البند الآن** لتفادي إنشاء محتوى شبه مكرر قد يضر الموقع بدل أن يفيده.

**⚠️ للمنفّذ التالي — لا تنفّذ هذا البند إلا بعد التأكد من توفر أحد الأمرين فعلياً:**
1. مشروع (أو أكثر) موثّق في `posts.json`/`works.html` منفَّذ فعلياً في منطقة الضجيج أو الفروانية تحديداً (يمكن الاستدلال عليه من اسم الملف/الوصف).
2. تقييم عميل حقيقي في نظام التقييمات مرتبط بإحدى هاتين المنطقتين.

إذا توفر أحدهما، يُبنى محتوى صفحة المنطقة **حول** ذلك الدليل الحقيقي تحديداً (وليس نصاً عاماً)، بدءاً بمنطقة واحدة فقط أولاً.

**البند 14 (فيديوهات/عروض/humans.txt):** لم يُنفَّذ أيضاً — الخطة نفسها تصنّفه كبند لا يؤثر مباشرة على ترتيب SEO/GEO (تفاعل زوار فقط)، ومشروط بإنتاج محتوى فعلي (تصوير) أو التزام تحديث مستمر غير متوفر حالياً.

---

## 🔜 ماذا بعد — الخطوة التالية في ترتيب التنفيذ (القسم 8 من الخطة)

الترتيب المتبقي حسب القسم 8 من `seo-v0.6-improvement-plan.md`:
2. ✅ (تم) إضافة `reviews.html` لـ sitemap + noindex لـ logo/watermark
3. ✅ (تم) GA4 + أحداث phone_click/whatsapp_click على الصفحات الست
4. ✅ (تم) صفحة "عن سالفورد" الكاملة + `sameAs` (إنستقرام/تيك توك حقيقيين) + توحيد NAP + ربط داخلي + sitemap
5. ✅ (تم) صفحات منتجات مستقلة بـ Product schema (7.2) — 33 صفحة مفهرسة + 4 صفحات `noindex` بانتظار أوصاف حقيقية من صاحب الموقع
6. ✅ (تم فحصه، لا حاجة لتعديل) تقصير عنوان index.html + `loading="lazy"` (3.5)
7. ✅ (تم، بالحدود الممكنة) ضغط الصور + WebP + أسماء وصفية + Image Sitemap (3.2 + 6.6 + 7.5) — راجع القسم 14 لتفاصيل i.ibb.co
8. ✅ (تم) توسيع المحتوى النصي لباقي الصفحات (3.3 + 5.3 + 6.4) — راجع القسم 15
9. ✅ (تم) HowTo schema + تعميم Breadcrumb (7.3 + 7.4) — راجع القسم 16
10. ✅ (تم) صفحة FAQ عامة مستقلة (6.2) — راجع القسم 17 أعلاه
11. ✅ (تم الإنشاء، بانتظار مراجعة بشرية قبل النشر) مركز المعرفة/المدونة (6.5 + 7.6) — راجع القسم 18 أعلاه
12. ✅ (تم) إضافة `llms.txt` تجريبياً (5.4) — راجع القسم 19 أعلاه
13. ⏸️ (مؤجَّل بقرار صريح) صفحات استهداف محلي بحذر (6.7) — راجع القسم 20 أعلاه لشروط التنفيذ لاحقاً
14. بنود مستقبلية اختيارية منخفضة الأولوية (فيديوهات، عروض، humans.txt...) — لم يُنفَّذ، ليست أولوية

---

## ملف Search Console
مرفق في هذه الحزمة: `salfordkw_shop-Performance-on-Search-2026-08-05.xlsx` — هذا هو ملف تصدير الأداء الفعلي من Google Search Console الذي استُند إليه في تحليل القسم 1 من `seo-v0.6-improvement-plan.md`. أي تحليل أو مقارنة لاحقة لبيانات الأداء (قبل/بعد تنفيذ بنود الخطة) يجب أن تُقارَن بهذا الملف كخط أساس (baseline).
